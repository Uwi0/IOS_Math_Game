import SwiftUI

struct AdditionGameView: View {
    var body: some View {
        ZStack {
            GameBackgroundView()
            Text("AdditionGameView")
        }
    }
}

#Preview {
    AdditionGameView()
}
